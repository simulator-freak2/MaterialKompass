name: materialkompass_workspace
description: Workspace entrypoint for the MaterialKompass Flutter app.
publish_to: 'none'
environment:
  sdk: '>=3.9.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  materialkompass:
    path: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
